# Hetzner Cloud static inventory — generated by ansible-role-hetzner-cloud
# Used when hcloud_write_inventory: true and hcloud_inventory_path is set.
# Prefer the hetzner.hcloud dynamic inventory plugin for stateless hand-off.
#
# Phase 4: this template will be populated from hcloud_provisioned_servers
# registered facts grouped by server labels (role, cluster).

# [control_plane]
# Phase 4 placeholder — servers with labels.role=control-plane

# [workers]
# Phase 4 placeholder — servers with labels.role=worker

# [proxy]
# Phase 4 placeholder — servers with labels.role=proxy
